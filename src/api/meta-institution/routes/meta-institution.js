'use strict';

/**
 * meta-institution router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::meta-institution.meta-institution');
